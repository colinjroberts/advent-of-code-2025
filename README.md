# advent-of-code-2025

https://adventofcode.com/

Now in rust!

In Rust, `main.rs` is always the entry point, so start there. Each day has its own file.

Run with `cargo run -- $day#-part#`, e.g. `cargo run -- 1-2` or `cargo run -- 10-1`.
